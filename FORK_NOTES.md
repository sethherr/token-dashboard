# Fork notes

> **Last updated:** 2026-07-23 (v0.7.0: **Tips tab redesign** — the detectors that flag many similar items (repeated file reads, repeated Bash commands, low cache efficiency, Opus-heavy projects, cross-workspace access, oversized/stacked `CLAUDE.md`, context-heavy sessions, repeated Bash errors, Bash output bloat, heavy web-fetch sessions, subagent outliers and return bloat) now render as **one collapsible section per category** instead of one card per item: the shared what/why explanation is shown once, and the concrete instances are listed collapsed below it — each stacked as a bold identifier, its numbers, and a drill-down link, with a per-instance dismiss. The six single-item tips (right-size, tool-bloat, skill-budget, dead-skills, MCP-sprawl, overlong descriptions) render in the same section style. Plus a batch of 12 internal code-review correctness fixes: manual **Refresh now** resolves the projects dir before scanning (previously a silent no-op when no path override was set); the 1d/2d/3d Overview/Skills ranges bypass the day-bucketed summary fast path so sub-day totals are accurate; SSE scan/error events fan out to every open `/api/stream` tab (was single-consumer); static-file containment hardened via `relative_to`; MCP usage counted by exact `mcp__<server>__` segment (no cross-attribution between similarly-named servers); Windows agent-rescan matches transcripts by basename; chart tooltips and the remaining frontend string interpolations are HTML-escaped; the Skills default cost sort keeps subagent-free skills correctly ordered; the duplicated `_read_json` is consolidated into a shared `jsonutil` with a dict-type guard; `writeRange`/`writeSort` use a hardcoded route base. v0.6.0: new **Register** nav group with **Plugins**, **MCP**, and **Hooks / Commands / Agents** catalog pages, skill descriptions on the Skills tab, 1d/2d/3d short range filters on Overview and Skills, and a clarifying tooltip on the Overview Est. cost tile noting it's the API pay-per-token value, not your subscription bill — integrates upstream PR #29 (galleries + nav, partial) and PR #31 (short ranges). v0.5.0: Claude Sonnet 5 pricing support. v0.4.0: plain-language, collapsible in-app metric explanations across Prompts / Skills / Subagents / Workspaces, a `<synthetic>`-row note, and a clarified Settings plan text; corrected Team plan monthly prices. v0.3.1: scanner per-content-block accounting fix) — see [recent commits](https://github.com/muckybuzzwoo/token-dashboard/commits/main) for the running change log.

This fork integrates community pull requests that have not been merged into the upstream [nateherkai/token-dashboard](https://github.com/nateherkai/token-dashboard) repository, because upstream is currently inactive.

Each integration was reviewed for security concerns (OWASP-style checks for SQL injection, path traversal, command injection, network exfiltration, dependency hygiene) and a passing test suite (`python -m unittest discover tests`).

## Integrated upstream PRs

| PR | Title | Author | Highlights |
|---|---|---|---|
| [#9](https://github.com/nateherkai/token-dashboard/pull/9) | `chore(ci): run tests on push and PR` | sebastientang | GitHub Actions workflow that runs the test suite on Python 3.8 / 3.10 / 3.12. |
| [#2](https://github.com/nateherkai/token-dashboard/pull/2) | Scanner progress (FirstTimeScanner) | lucidqdreams | Live per-file progress on stderr during scans, throttled to ~5 updates/sec, tty-aware. |
| [#10](https://github.com/nateherkai/token-dashboard/pull/10) | CSV and Markdown export for the Prompts route | sebastientang | "Copy MD" copies the current sort/filter to the clipboard. "Download CSV" produces a properly escaped CSV. |
| [#15](https://github.com/nateherkai/token-dashboard/pull/15) | Pricing fix + team plan labels | hacker4ofakind | Corrects Opus 4.5 / 4.6 / 4.7 pricing to current Anthropic rates ($5 input / $25 output, with matching cache rates). Also adds `team` / `team-premium` plan labels for the Settings dropdown. **Before this fix the dashboard showed ~3× inflated Opus costs.** |
| [#12](https://github.com/nateherkai/token-dashboard/pull/12) | Skills budget attribution + slash-command scanner | sebastientang | Adds budget-vs-actual tracking (p50 / p95 output tokens), total cost, and Task/Agent-dispatched subagent attribution to the Skills route. Synthesizes a `Skill` row from user-typed `<command-name>/<slug>` messages so slash-command usage appears in the analytics. Supersedes PRs #6 and #11. |
| [#7](https://github.com/nateherkai/token-dashboard/pull/7) | Configure Claude folder settings (UI) | kiwiswift | Settings tab can now switch the `.claude` folder at runtime. Persists the selection in the SQLite DB. Includes an opt-in "clear cached transcript data" toggle for clean separation across profiles. |
| [#13](https://github.com/nateherkai/token-dashboard/pull/13) | Large-database performance + optional RTK savings view | NewAiCoder | Materialised summary tables (daily / per-project / per-model / tools / sessions), WAL journal mode, batched scanner commits, in-process response cache, synchronous warm of the default 30-day range before serving the first request, sortable tables, URL-persisted Sessions filters, optional RTK savings tab when the [rtk](https://github.com/rtk-ai/rtk) CLI is installed at `~/.local/bin/rtk` (the tab gracefully degrades on Windows or when RTK is absent). |
| [#18](https://github.com/nateherkai/token-dashboard/pull/18) | `perf(scanner): batch inserts + bulk snapshot eviction (~33x on 1 GB)` | Dig1taly | Replaces the per-message INSERT/DELETE loop in `scan_file()` with batched `executemany()` and a single temp-table-driven snapshot eviction. Composes with this fork's PR #13 file-level batched commits — they operate on different layers. The fork-specific adaptation preserves the `days` / `sessions` sets `scan_dir()` needs for incremental rebuild of the materialised summary tables. Measured locally: a full rescan of 589 files / 63k messages drops from minutes to **~7.5 seconds**. |
| [#19](https://github.com/nateherkai/token-dashboard/pull/19) | `Skills: Track 'command' usage` | wolff-singleton | Stores Claude Code's native `attributionSkill` field on every assistant message into a new `messages.attribution_skill` column, and splits the Skills route into "You ran" (distinct sessions where you typed a slash command) vs. "Claude invoked" (Skill tool calls Claude emitted itself, typically from Task/Agent subagents). The page is renamed to "Skills & Commands" and gains two KPI tiles + two table columns matching. Includes a fork-specific de-dupe filter: the merged `skill_breakdown` query's `tool_inv` CTE adds `AND EXISTS (… messages WHERE uuid=tc.message_uuid AND type='assistant')` so the synthesised slash-command rows that PR #12 writes on user-type messages don't double-count against the new `attribution_skill` totals. The fork's existing budget / p50 / p95 / total-$ / total-inc-subagents columns from PR #12 are preserved. Migration clears `messages` / `tool_calls` / `files` / `summary_meta` so the next scan replays JSONLs and rebuilds summaries from scratch. |
| [#29](https://github.com/nateherkai/token-dashboard/pull/29) | Gallery dashboard (Plugins/MCP/Hooks), nav grouping, plan usage panel | fesak7 | **Partial integration — additive gallery + nav only.** Adds a **Register ▾** nav group (holding Skills, Plugins, MCP, Hooks) and three read-only local catalog scanners: `plugins.py` (installed plugins + enabled state + component/token counts), `mcp_catalog.py` (MCP servers), `hooks_catalog.py` (hooks / commands / agents), each with its own GET endpoint and a vanilla-JS page. Also parses a `description` from every `SKILL.md` frontmatter and shows it on the Skills tab. All scanning is manifest-driven and read-only over `~/.claude/`. **The PR's remote/quota machinery (`plan_usage.py`, `quota.py`, `alerts.py`, `skill_tokens.py`) was deliberately excluded** — see the skipped-parts row below. Upstream PR is CLOSED (author continuing on their own fork); integrated 2026-07-22 after security audit, per-endpoint tests, and a browser smoke test. Includes fork-specific cross-platform fixes: `.ps1` hook detection, Windows drive-letter preservation in the hook script-path regex, and `.as_posix()` for all editor-link paths. |
| [#31](https://github.com/nateherkai/token-dashboard/pull/31) | `feat: add 1d/2d/3d short time-range filters` | RKdev | Adds 1-, 2- and 3-day buttons to the Overview and Skills range selectors, alongside 7d/30d/90d/all. Fork fix: the selector's default was position-based (`RANGES[1]`), so prepending the short ranges would have shifted the default from 30d to 2d — changed to look up `30d` by key instead. |
| [#22](https://github.com/nateherkai/token-dashboard/pull/22) | `Add Workspaces + Subagents tabs` | marcinkordas | Adds two new dashboard tabs. **Workspaces** renders a bipartite ECharts Sankey of agent cwd → file target (Read/Edit/Write/NotebookEdit only) plus a ranked cross-workspace leaks table with top files per (source, target) pair — surfaces sessions that cross CLAUDE.md boundaries. **Subagents** splits per-model spend into main thread vs Task-subagent vs auto-compaction (agent_id `acompact-*`), surfaces SDK-entrypoint orchestration clusters, and reconstructs a dispatcher → child agent dispatch tree via session_id + timing-join. Backend adds 7 new query functions in `db.py`, a `sankeyChart()` helper in `web/charts.js`, and 3 new endpoints (`/api/workspaces`, `/api/cross-workspace-leaks`, `/api/subagents`). A new tip rule (`cross_workspace_tips`) flags agent-X-reads-workspace-Y > 50 calls in 7d. **Fork modification: the author-specific tier-routing panel (hardcoded `orchestrate` / `agentic-efficiency-harness` / `pareto-cascade-delivery` / `run-benchmark` skill list) was stripped pre-integration** — the upstream PR's `tier_routing_stats()`, `tier_routing_gap_tips()`, and the 4-column KPI tile collapse to a 3-column row because none of those skills are used by this fork. The upstream PR was CLOSED ("drafted from fork" by the author) and never had upstream review; integrated 2026-05-28 after dependency-shape verification and Playwright smoke against `/workspaces`, `/subagents`, and `/sessions/<id>`. |

## Community PRs to this fork

| PR | Title | Author | Highlights |
|---|---|---|---|
| [#1](https://github.com/muckybuzzwoo/token-dashboard/pull/1) | Fix skill-budget tip to count active skills only | sant3e (Diaconescu Tiberiu) | Three correctness fixes for the skill-budget tip: (1) `enabledPlugins` in `~/.claude/settings.json` now filters out installed-but-disabled plugins; (2) skills with `disable-model-invocation: true` in their SKILL.md frontmatter are excluded (model never sees their description); (3) skills hidden via `skillOverrides: user-invocable-only / name-only / off` are excluded. Also adds symlink-safe `_iter_skill_files()` replacing `Path.rglob()` in `scan_catalog()` with `os.walk(followlinks=True)` + cycle detection. |
| [#2](https://github.com/muckybuzzwoo/token-dashboard/pull/2) | Updated pricing for Opus 4.8 | hacker4ofakind | Adds `claude-opus-4-8` to `pricing.json` at $5/$25 input/output MTok (same tier as Opus 4.7). Integrated directly via `git pull` on 2026-05-29. |
| [#4](https://github.com/muckybuzzwoo/token-dashboard/pull/4) | `fix(tips): name source tools in tool-bloat tip body` | sant3e (Diaconescu Tiberiu) | The tool-bloat outlier tip now names the offending tools (e.g. *"Mostly from Read (12), WebFetch (1)"*) by joining `_tool_result` rows to their invoking `tool_use` rows via `tool_use_id`, so the suggested remediation is actionable. Falls back gracefully (no extra sentence) on old data without `tool_use_id` linkage. |
| [#5](https://github.com/muckybuzzwoo/token-dashboard/pull/5) | `fix(tips): measure subagent return payload, not internal burn` | sant3e (Diaconescu Tiberiu) | The subagent-sprawl tip previously flagged sessions on the sidechain-vs-main token ratio, conflating internal subagent work (the point of delegation) with cost paid by the parent. It now flags the actual **return payload**: joins `Agent`/`Task` calls to their `_tool_result` via `tool_use_id` and flags sessions whose returns sum ≥20k **and** average ≥5k per dispatch. Fixes a real false positive where a subagent burned 77k internally but returned only 1.7k into main. `SubagentSprawlTests` rewritten around the new signal. |
| [#6](https://github.com/muckybuzzwoo/token-dashboard/pull/6) | `feat(sessions): cost column + filter UI with live totals` | epicvinny | Adds a per-session **cost** column to the Sessions tab plus a full client-side filter bar — project autocomplete (custom bounded menu replacing the native `<datalist>`), project/session search, quick date ranges (All/Today/7d/30d/90d) and a custom `datetime-local` from/to range, and min-$/min-tokens thresholds — all debounced and URL-hash-persisted. A totals footer recomputes live as filters change, with a `*` marking tier-estimated pricing. Backend adds `session_model_tokens()` in `db.py` and decorates `/api/sessions` with `cost_usd`/`cost_estimated` (summing `cost_for(...)` per model the session used). The list now loads up to 500 sessions (was 100). No new dependencies. |
| [#3](https://github.com/muckybuzzwoo/token-dashboard/pull/3) | `fix: made blur work and hide private costs and info` | hacker4ofakind | Makes the privacy-blur feature actually conceal content: stronger CSS (transparent text + text-shadow + `pointer-events:none`, previously a 6px blur that left text guessable), a topbar **blur on/off** toggle (localStorage-persisted, with a `Ctrl/Cmd/Alt+B` keyboard fallback since some OSes intercept the shortcut), and `blur-sensitive` markers added across all tabs (costs, project names, session ids, file paths, charts). Merge conflict in `web/routes/sessions.js` against PR #6 resolved by keeping #6's cost column + filter structure and applying #3's blur policy to the project name, the row + totals cost cells, and the session id. |

## Deliberately skipped upstream PRs

| PR | Reason |
|---|---|
| [#4](https://github.com/nateherkai/token-dashboard/pull/4) | Adds Claude Code remote session-init hooks and tracks `.claude/settings.json`. Useful only for Claude Code remote (web) sessions, which this fork's owner does not use; would also start tracking project-local Claude settings that should stay private. |
| [#14](https://github.com/nateherkai/token-dashboard/pull/14) | Subset of #15 (pricing fix only); #15 includes the same fix plus the team plan additions. |
| [#17](https://github.com/nateherkai/token-dashboard/pull/17) | A less polished alternative to #2 with the same purpose (scan progress callback). PR #17 was reopened on 2026-05-12 with the callback signature `(scanned, total, totals)` — strictly less information than this fork's PR #2 signature `(index, total, path, totals)`. Integrating it would degrade the CLI progress printer. |
| [#6](https://github.com/nateherkai/token-dashboard/pull/6), [#11](https://github.com/nateherkai/token-dashboard/pull/11) | Both fully contained within #12. |
| [#27](https://github.com/nateherkai/token-dashboard/pull/27) | Evaluated 2026-07: adds a `TOKEN_DASHBOARD_SCAN_INTERVAL` env var (server-side only). Not integrated — the fork intentionally couples the 60s scan cadence between server (`_scan_loop`) and client (`SCAN_INTERVAL` in `web/app.js`); a server-only patch would desync the UI countdown. No user need for a configurable interval at present. |
| [#28](https://github.com/nateherkai/token-dashboard/pull/28) | Evaluated 2026-07: adds Fable 5 + aligns Opus to $5/$25. Fully superseded — the fork already carries `claude-fable-5`, `claude-opus-4-8`, and the 5/25 Opus rates, plus `claude-sonnet-5` which this PR predates. |
| [#29](https://github.com/nateherkai/token-dashboard/pull/29) — *remote/quota parts* | The gallery + nav portion is integrated (above); the remote/quota machinery was left out on purpose. `plan_usage.py` reads the Claude Code OAuth refresh token from disk, copies it into the dashboard's own DB, self-refreshes it, and calls `api.anthropic.com/api/oauth/*` on every Overview load with no opt-out — a direct violation of this fork's fully-local / no-telemetry rule. `quota.py` (local rolling-window quota) was backend-only in the PR, with the Overview card and Quota tab both wired to `plan_usage.py`'s remote endpoint instead — left out entirely rather than building a replacement UI. `alerts.py` (its only call site was already commented out by the PR author), `skill_tokens.py` / `/api/skills_cost`, and the PR's own `GOAL.md` / `IMPLEMENTATION-LOG.md` / `docs/superpowers/*` planning artifacts were also not taken. |
| [#13](https://github.com/nateherkai/token-dashboard/pull/13) — *further upstream commits since `bea4199`* | Integration of PR #13 is locked to commit `bea4199`. Between then and `300160a` (the current PR head) the upstream branch picked up 17 additional commits in author-specific directions: a billable-hours / client-velocity toolkit, hands-on-time tracking per project, lanes / prompt-search / calendar-heatmap views, JLAP PM2 deploy config, and multi-host scanning. None match this fork's product direction and the upstream PR is CLOSED. No further sync from `upstream/pr/13` is planned. Individual fix commits (`fix(scanner): self-healing drift detection`, `feat(projects): fix wrong-cwd naming bug`, `fix: Codex review batch`) may be cherry-picked à la carte on request, but conflicts are likely because the fork's `db.py` / `scanner.py` evolved in parallel. |

## Differences from upstream

- The Skills route is renamed **Skills & Commands** and splits invocations into two distinct counts: "You ran" (distinct sessions where you typed `/slash-command`, tracked via Claude Code's native `attributionSkill` field on assistant turns) and "Claude invoked" (real `Skill` tool calls Claude emitted, typically from `Task`/`Agent`-dispatched subagents). Both sources are de-duplicated against each other so the same slash command never inflates both columns.
- Skills tab shows declared output-token budget vs measured p50 / p95, and flags skills that exceed budget by more than 20%.
- Overview-style endpoints (`/api/overview`, `/api/projects`, `/api/sessions`, etc.) read from materialised summary tables, so the dashboard stays responsive at hundreds of thousands of messages.
- Settings tab can switch the `.claude` folder at runtime.
- Prompts tab can export the current view to CSV or Markdown.
- Scanner uses batched `executemany()` inserts and a single temp-table-driven snapshot eviction pass per file (PR #18); a full rescan of ~600 files / 60k+ messages completes in well under 10 seconds on commodity hardware.
- Scanner accounts for the **current per-content-block transcript format** (v0.3.1, from the analysis in [nateherkai#25](https://github.com/nateherkai/token-dashboard/issues/25) by @sdkkds). Claude Code now splits one assistant response into disjoint records that share a `message.id` and each repeat the final `usage` (not growing snapshots). The snapshot dedup still collapses these to one message row (token totals match billing), but **re-points the evicted siblings' `tool_calls` onto the surviving keeper** (deduped by `tool_use_id`) instead of dropping them — so parallel `tool_use` blocks are no longer lost from Top Tools / Skills counts. Turn counts (`overview`, `projects`, `sessions`, summary tables) now filter `prompt_text IS NOT NULL`, excluding tool-result-as-user records that previously inflated "turns" ~6–8×. Verified token-neutral and idempotent across full rescans on real transcripts. Token/cost figures are unaffected. (Existing DBs need one rescan to pick up the recovered tool rows.)
- One-time CLI helpers: `python cli.py rescan-agent-targets` (back-fill `Agent` rows that were ingested before the `Task→Agent` rename) and `python cli.py rescan-slash-commands` (synthesise `Skill` rows from historical slash-command user messages — kept as a fallback for DBs that pre-date the `attribution_skill` column).
- The optional RTK savings tab probes `~/.local/bin/rtk` at request time and shows install instructions when absent; on Windows it always shows the install state because that path does not exist.
- Two attribution tabs from PR #22: **Workspaces** (bipartite Sankey of agent cwd → file target + cross-workspace leaks table) and **Subagents & Orchestration** (per-model main / Task-subagent / auto-compaction split, SDK-entrypoint runs, dispatcher → child dispatch tree). New tip rule fires when an agent in workspace X reads files in workspace Y > 50 times in 7 days. The upstream PR's hardcoded tier-routing skill panel (`orchestrate` / `agentic-efficiency-harness` / `pareto-cascade-delivery` / `run-benchmark`) was stripped because this fork's owner does not use those skills — the corresponding `tier_routing_stats()` query, `tier_routing_gap_tips()` rule, and the first KPI tile in the Subagents page are absent in this fork.
- Session detail page (`#/sessions/<id>`) header shows the **full** session UUID, not a truncated `4edcfece…` prefix — useful when copying the ID for bug reports or cross-referencing JSONL files. Listings (Sessions tab, Top sessions tables) keep the 8-character prefix to save horizontal space.
- **Plain-language, collapsible in-app explanations (v0.4.0)** — every data-heavy tab now carries a collapsible legend built on the same `<details>` glossary pattern as the Overview's "What do these numbers mean?": Prompts, Skills & Commands, Subagents (agent kind, entrypoint, dispatch tree, top sessions, per-(kind, model) table) and Workspaces. The previously dense, jargon-packed column/chart descriptions were rewritten into scannable *term → definition* lists with concrete examples and a "what you can read out of it" framing, collapsed by default with a "— click to expand" hint. Also adds a note explaining `<synthetic>`-model rows on the Prompts tab (Claude Code's auto-injected "Continue from where you left off." continuations after a resume / auto-compaction — they carry token counts but no price), and clarifies in Settings that picking a subscription plan never changes the displayed dollar figures (always the API pay-per-token value) — it only shows your flat monthly fee alongside for comparison.
- **Tips engine expansion (2026-05-28)** — 10 additional rule-based tip detectors layered on top of the existing cache / repeat-file / repeat-bash / right-size / tool-bloat / subagent-outlier / cross-workspace / skill-budget / claude-md-size rules:
  - `dead-skills` — installed skills with zero `Skill`-tool invocations in 90 days, excluding those installed in the past 30 days (file mtime as install-age proxy). Fires when at least 5 dead skills are found.
  - `subagent-sprawl` — sessions where sidechain (subagent) tokens exceed 2× main-chain tokens with >50k sidechain total in 7 days, excluding `acompact-*` auto-compaction.
  - `bash-bloat` — Bash commands averaging >5k tokens of output without an obvious output limiter (`head`, `tail`, `wc -l`, `Select-Object -First`, `-TotalCount`, …). Cross-shell heuristic. Requires the new `tool_calls.tool_use_id` column to join command rows to their `_tool_result` rows.
  - `context-pressure` — sessions whose peak per-turn net-new tokens (`input + cache_create_5m + cache_create_1h`, deliberately excluding `cache_read_tokens` because Anthropic multi-counts cache reads across breakpoints) exceeds 100k.
  - `bash-errors` — Bash commands that errored ≥3 times in 3 days with identical command text.
  - `web-fetch-volume` — sessions with ≥15 web-fetch calls in 7 days. Matches `WebFetch` and any `mcp__server__tool` whose name carries `fetch` / `read_url` / `scrape` / `crawl` / `browser_navigate` — no specific brand (Jina, Firecrawl, …) is hard-coded.
  - `opus-only` — projects where >90% of assistant turns in 14 days ran on Opus, with at least 50 turns to filter noise.
  - `mcp-sprawl` — ≥12 distinct MCP servers active in 7 days.
  - `claude-md-stack` — a cwd whose ancestry contains ≥3 CLAUDE.md files totalling >400 lines.
  - `long-skill-descriptions` — ≥3 installed skill descriptions exceed 400 chars.
- **Strict plugin-slug match in the skill catalog (2026-05-28)** — `_slugs_for()` in `skills.py` previously registered every non-structural ancestor path segment as a possible `<plugin>:<skill>` prefix. On Windows the home path leaked `Users` and the username into the catalog as fake plugin names; on either OS the marketplace directory name was indistinguishable from a real plugin. The catalog now anchors strictly on the two documented on-disk layouts (`marketplaces/<m>/plugins/<plugin>/skills/...` and `cache/<m>/<plugin>/<version>?/skills/...`) plus a plugin-root `SKILL.md` fallback when the parent directory matches a version pattern. The `skill_listing_budget_tips` candidate list dedupes by `SKILL.md` path and prefers the plugin-qualified form for display.
- **`tool_calls.tool_use_id` column (2026-05-28)** — additive schema migration linking each tool_use row to its matching `_tool_result` row via the `tool_use_id`. Required for `bash-bloat` and `bash-errors` per-command attribution. Migration auto-runs on next `init_db()` (server start) and is idempotent. As with prior migrations, the data tables (`messages`, `tool_calls`, `files`, `summary_meta`) are cleared so the next scan replays JSONLs with the new column populated — source of truth lives on disk, full rescan is ~10 seconds on commodity hardware.
- **Scope-aware skill catalog (2026-05-28)** — `scan_catalog()` previously rglobbed every `SKILL.md` under `~/.claude/plugins/`, including marketplace clones (downloaded plugin metadata that was never installed). Those phantoms inflated the `skill-budget`, `dead-skills`, and `long-skill-descriptions` tips. The catalog now reads `~/.claude/plugins/installed_plugins.json` and only scans the `installPath` of each currently-installed plugin; user-skills (`~/.claude/skills/`), scheduled-tasks, and project-local `.claude/skills/` discovered from `messages.cwd` are still included. Each catalog entry now carries a `scope` (`user-global` / `project-global` / `project-local` / `unknown`) and a `project_path`, so tips can reason per-session: `skill_listing_budget_tips` now reports the **effective per-session footprint** intersected with the user's most-active recent cwd, splits "N global skill(s) plus M project-scoped skill(s) load per session" in the body, and names the project. `dead_skills_tips` skips project-scoped skills whose `projectPath` had zero recent activity (those zero invocations are meaningless — the user simply hasn't worked in that project). Legacy blanket scan still fires when the manifest is missing/unreadable, but **not** when it is valid-but-empty (the user has uninstalled everything; clones must stay excluded). Bounds-check on `installPath` rejects filesystem roots to neutralise a tampered-manifest rglob DoS. Live impact on a heavy user's machine: catalog 448 → 208 slugs, skill-budget tip reports ~18.9k effective chars instead of inflated all-on-disk counts.
- **`enabledPlugins` + skill-visibility filtering in budget tip (2026-05-29)** — `_read_installed_plugin_entries()` now reads `~/.claude/settings.json`'s `enabledPlugins` dict and skips plugins whose entry is `false` (installed but disabled). `skill_listing_budget_tips()` now skips skills with `disable-model-invocation: true` in their frontmatter, or whose slug is set to `user-invocable-only` / `name-only` / `off` via `skillOverrides` in global or project `settings.local.json`. Both changes prevent false-positive budget warnings for skills the model never actually loads. `scan_catalog()` replaces `Path.rglob()` with `_iter_skill_files()` using `os.walk(followlinks=True)` + cycle detection so symlinked project skill directories are correctly discovered. Community [PR #1](https://github.com/muckybuzzwoo/token-dashboard/pull/1).
- **Prompts tab re-linked to assistant turns (2026-06-10)** — a newer Claude Code version changed the transcript format: it now writes `attachment` / `queue-operation` / `last-prompt` records and re-parents the assistant turn through an `attachment` node, so `assistant.parent_uuid` no longer points at the typed prompt (and assistant rows carry no `prompt_id`). The old `expensive_prompts` join (`JOIN messages a ON a.parent_uuid = u.uuid`) silently dropped to **0 matches** from ~late May onward, so the Prompts tab appeared frozen at 2026-05-29 and never showed Opus 4.8 (the model used from June). The DB and every aggregate view (Overview, per-model, Projects, Sessions) were always correct — only this one join broke. Rewrote `expensive_prompts` to link by **session + time window** instead of `parent_uuid`: take the typed prompt per `(session_id, prompt_id)` (earliest row — collapses the injected `list[text]` skill/system messages that share the prompt's `prompt_id`), main-thread only (`COALESCE(is_sidechain,0)=0`), and attribute every main-thread assistant message between it and the next prompt. `billable_tokens` is now the full turn's cost (incl. its tool loop), not just the first response. Pure query change — no schema migration or re-scan needed. Regression tests cover the `attachment` interposition and the shared-`prompt_id` collapse.
- **Prompts tab: NULL-model first assistant no longer drops the prompt (2026-06-15, v0.2.1)** — follow-up correctness fix to the session+time-window rewrite above. The displayed-model subquery picked the *earliest* assistant in the window with no `model IS NOT NULL` guard; combined with the outer `WHERE model IS NOT NULL` filter, a leading API-error / synthetic assistant row (`model` NULL — written by the scanner whenever `message.model` is absent, e.g. an error turn before a successful retry) caused the **entire** prompt to be dropped from the Prompts tab, including the billable tokens of the real response that followed. Fixed by skipping NULL-model rows in the model-pick subquery only (`... WHERE {win} AND a.model IS NOT NULL ...`); the token SUMs still span the full turn, so an error row's (usually zero) tokens are still counted. Regression tests added for the NULL-model-first ordering and for multi-assistant (tool-loop) summing. Pure query change — no schema migration or re-scan needed.
- **Fable 5 model support (2026-06-10)** — adds `claude-fable-5` to `pricing.json` as a new tier **above** Opus at **$10 / $50** input/output per MTok (cache rates $1.00 read / $12.50 5m-write / $20.00 1h-write, derived from Anthropic's standard 0.1× / 1.25× / 2× input multipliers). The tier matcher (`_tier_from_name` in `pricing.py`), the frontend badge classifier (`fmt.modelClass` in `web/app.js`), and a new `.badge.fable` style (gold, distinct from Opus-purple / Sonnet-blue / Haiku-green) all learned the `fable` tier, and a `fable` `tier_fallback` entry prices future dated/suffixed variants instead of returning a null cost. Opus 4.8 needed no work — it was already integrated via community [PR #2](https://github.com/muckybuzzwoo/token-dashboard/pull/2) and Claude Code persists it to the JSONL as the bare `claude-opus-4-8` (the `[1m]` context annotation is not written to transcripts), so it already matches the exact-pricing entry.
- **Sonnet 5 model support (2026-07-03)** — adds `claude-sonnet-5` to `pricing.json` at the standard Sonnet-tier list price (**$3 / $15** input/output per MTok; cache rates $0.30 read / $3.75 5m-write / $6.00 1h-write), identical to `claude-sonnet-4-6`. Unlike the Fable 5 addition this needed **no new tier plumbing** — the `sonnet` tier already exists in `_tier_from_name` (`pricing.py`), the `fmt.modelClass` badge classifier (`web/app.js`), the `.badge.sonnet` style, and the `sonnet` `tier_fallback` — so the only effect of the exact `models` entry is reporting `estimated: false` instead of the tier-fallback's `estimated: true`. Anthropic's current **$2 / $10 introductory rate (through 2026-08-31) is intentionally not encoded**: `pricing.json` has no time-windowed pricing, all entries use list prices, and an intro rate would be wrong after the promo window.
- **Local-timezone time display (2026-05-29)** — all timestamp rendering now uses the **browser's local timezone** instead of a hardcoded `America/New_York` (inherited from upstream). `fmt.ts` (Sessions list/header, Prompts, Subagents, Skills) dropped the `timeZone: 'America/New_York'` override; a new `fmt.time` helper renders the Sessions turn-by-turn time column, which previously showed **raw UTC** (`timestamp.slice(11,19)`) and so disagreed with the session header. Both now use `toLocaleString` / `toLocaleTimeString` with the `'sv'` locale for ISO-style `YYYY-MM-DD HH:MM:SS` formatting. The turn-by-turn `data-val` keeps the raw UTC ISO string, so the click-to-sort key stays chronological across day boundaries. The RTK "Today" card's `today()` and the Prompts CSV filename stamp likewise switched from `America/New_York` / UTC to browser-local. **Deferred:** server-side daily-chart bucketing still groups by **UTC** calendar day (`substr(timestamp, 1, 10)` in `db.py`), so a turn after local midnight can land on the previous day's bar; converting that to local-day bucketing (touches the `idx_messages_date` index, `_date_range_clause`, and the bundle cache key) is a separate change not done here.
- **Register nav group + local catalog pages (2026-07-22, v0.6.0)** — the top nav gains a **Register ▾** dropdown grouping four read-only "what's installed" views. **Plugins** lists every plugin in `~/.claude/plugins/installed_plugins.json` with its enabled state, source marketplace, version, component counts (skills / agents / commands) and an approximate always-on token cost. **MCP** lists configured MCP servers (see the dedicated bullet below). **Hooks / Commands / Agents** lists configured hooks from `settings.json` (with a broken-script check), user commands from `~/.claude/commands/`, and plugin-shipped commands/agents. Skills, Plugins, MCP and Hooks all live under the dropdown; the other tabs stay top-level links. Every catalog is manifest-driven (installed plugins only, marketplace clones and disabled plugins excluded — the same rule the skill catalog already uses) and read-only. Adapted from upstream PR #29; the PR's remote plan-usage / quota panels were not integrated.
- **Skill descriptions on the Skills tab (2026-07-22)** — `scan_catalog()` now parses `name` / `description` from each `SKILL.md`'s YAML frontmatter (a stdlib line parser, no PyYAML) and the Skills & Commands table shows the description under each skill name.
- **1d / 2d / 3d short range filters (2026-07-22)** — the Overview and Skills range selectors gain 1-, 2- and 3-day buttons (upstream PR #31). The default-range lookup changed from position-based (`RANGES[1]`) to a `30d`-by-key lookup so the new short ranges don't shift the default.
- **MCP catalog is fully auto-scanned, no hand-maintained file (2026-07-22)** — the MCP page lists local servers from `~/.claude.json` (top-level and project-scoped `mcpServers`) plus servers shipped by installed, enabled plugins (their `.mcp.json`), deduplicated by name (local wins) and sorted. Account-level claude.ai connectors (Gmail, Calendar, Slack) are intentionally **not** listed: they have no local config file, so they can't be read reliably — the only on-disk trace is a stale, incomplete "ever connected" name list. The dashboard shows only what it can scan from disk, so nothing here needs hand-maintenance. (Upstream PR #29 shipped a hand-edited `mcp_known_connectors.json`; this fork dropped it in favour of the plugin scan.) All catalog file paths use `.as_posix()` so the frontend's `vscode://file/...` editor links work on Windows, macOS and Linux.

## Local conventions for this fork's owner

- `start_dashboard.bat` (a Windows launcher that wraps `python cli.py dashboard`) is intentionally `.gitignore`d. It is per-user and should not be redistributed.
- The default port is still `8080`; override with `PORT=9000 python cli.py dashboard` if you run multiple dashboards.

## Pulling further upstream changes

If upstream resumes and you want to pull additional changes:

```bash
git fetch upstream
git merge upstream/main
# resolve conflicts; this fork has substantial changes in scanner.py / server.py / db.py
python -m unittest discover tests
```

The `upstream` remote's push URL is intentionally set to `DISABLED` to prevent accidental pushes to the upstream repository.
